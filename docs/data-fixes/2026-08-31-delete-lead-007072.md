# 2026-08-31 — Permanent delete of lead 007072 (owner request)

**What:** Lead `007072` — Αλωνιστιώτη Μαρία (info@kingmenelaos-mikines.com, 27510 76300), board `under_development`, stage `ud_parking`, source manual, €0 value, created 2026-08-27. Not won, not converted, 0 comments, 0 offers.

**How:** DML via the Supabase Management API (owner-run script), mirroring `delete_leads()` (20260618000005):
1. Row backed up to `public.leads_deleted_backup_20260831` (RLS on, no `authenticated`/`anon` grants) — 1 row.
2. Guard: refuses won/converted leads (same rule as the RPC).
3. `comments` (parent_type='lead') deleted — 0 rows.
4. `email_messages.lead_id` set NULL (captured Gmail archive is kept, only unlinked) — the FK from 20260710150000 has **no ON DELETE action**, which also means the first delete attempt failed with `email_messages_lead_id_fkey`.
5. `delete from leads` — verified `remaining=0`, `backed_up=1`, `still_linked_emails=0`.

**Latent bug found:** `public.delete_leads(uuid[])` (the admin Delete in the leads UI) hits the same FK error for ANY lead with captured emails — it deletes `comments` explicitly but not the `email_messages` link. Fix when picked up: add `update public.email_messages set lead_id = null where lead_id = any(v_deletable);` before the delete in the RPC (or `on delete set null` on the FK).

**Revert:** `insert into public.leads select (l).* from (select l from public.leads_deleted_backup_20260831 l where code = '007072') s;` — column-compatible since the backup was created from `leads` with only `deleted_at` prepended; re-link emails manually if needed. Drop the backup table once confident.
